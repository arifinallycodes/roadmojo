<%
msg = flash[:notice] ? flash[:notice] : flash[:error]
if flash[:notice]
  msg = flash[:notice]
  info_class = "alert-success alert"
else
  msg = flash[:alert]
  info_class = "alert-error alert"
end
# msg = "You have started following the places successfully"
#  if !@invite.errors.blank? 
#   msg = @invite.errors.full_messages.join(' ')
#   is_error = true
#  end

 # info_class = is_error ? "alert-error alert" : "alert-success alert"  
%>
$('.flash-messages').append("<div class='<%= info_class %>'> <a class='close' data-dismiss='alert'>×</a> <div><%= msg %></div> </div>");
